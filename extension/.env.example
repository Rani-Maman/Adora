# Extension environment variables (required)
# Copy this to .env and run: npm run build:config

# Backend API URL (required)
API_BASE=https://your-api-domain.com

# Risk threshold (optional, default 0.6)
RISK_THRESHOLD=0.6
